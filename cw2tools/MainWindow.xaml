<Window x:Class="cw2tools.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:cw2tools"
        mc:Ignorable="d"
        Title="Chrono Cross Tool" Height="700" Width="1100"
        Background="{StaticResource BackgroundColor}"
        WindowStartupLocation="CenterScreen">

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="220"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <!-- Sidebar -->
        <Border Grid.Column="0" Background="{StaticResource SurfaceColor}" BorderBrush="{StaticResource BorderColor}" BorderThickness="0,0,1,0">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <!-- Branding -->
                <TextBlock Grid.Row="0" Text="CW2 Tools" FontSize="20" FontWeight="Bold" Margin="20" Foreground="{StaticResource PrimaryColor}"/>

                <!-- Navigation -->
                <StackPanel Grid.Row="1" Margin="10">
                    <RadioButton Style="{StaticResource NavButtonStyle}" Content="首页" IsChecked="True" Checked="Nav_Home"/>
                    <RadioButton Style="{StaticResource NavButtonStyle}" Content="菜单编辑器" Checked="Nav_MenuEditor"/>
                    <RadioButton Style="{StaticResource NavButtonStyle}" Content="字体编辑器" Checked="Nav_FontEditor"/>
                    <RadioButton Style="{StaticResource NavButtonStyle}" Content="主界面编辑器" Checked="Nav_MainMenuEditor"/>
                    <RadioButton Style="{StaticResource NavButtonStyle}" Content="字符串编辑器" Checked="Nav_StringEditor"/>
                    <RadioButton Style="{StaticResource NavButtonStyle}" Content="Token 查找" Checked="Nav_TokenEditor"/>
                    <RadioButton Style="{StaticResource NavButtonStyle}" Content="键盘编辑器" Checked="Nav_KbdEditor"/>
                    <RadioButton Style="{StaticResource NavButtonStyle}" Content="代码注入" Checked="Nav_CodeInjector"/>
                    <!-- Debug was just opening CodeInjector in the original code? No, Button_Click_4 was CodeInjector. Button_Click_5 was KbdEditor. -->
                </StackPanel>

                <!-- Footer / Global Actions -->
                <StackPanel Grid.Row="2" Margin="10">
                    <Button Content="打开 Rom" Click="OpenRom" Margin="0,0,0,10"/>
                    <Button Content="保存 Rom" Click="SaveRom" Background="#27AE60"/>
                </StackPanel>
            </Grid>
        </Border>

        <!-- Content Area -->
        <Grid Grid.Column="1">
            <ContentControl x:Name="MainContent"/>
        </Grid>
    </Grid>
</Window>