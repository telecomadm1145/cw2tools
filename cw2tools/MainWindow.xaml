<Window x:Class="cw2tools.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:cw2tools"
        mc:Ignorable="d"
        Title="Chrono Cross Tool" Height="750" Width="1200"
        Background="{StaticResource BackgroundColor}"
        WindowStartupLocation="CenterScreen"
        FontFamily="Segoe UI">

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="240"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <!-- Sidebar -->
        <Border Grid.Column="0" Background="White" BorderBrush="{StaticResource BorderColor}" BorderThickness="0,0,1,0">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <!-- Branding -->
                <Border Grid.Row="0" Padding="25" BorderBrush="{StaticResource BorderColor}" BorderThickness="0,0,0,1" Margin="0,0,0,10">
                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                        <TextBlock Text="CW2" FontSize="24" FontWeight="Bold" Foreground="{StaticResource PrimaryColor}"/>
                        <TextBlock Text=" Tools" FontSize="24" FontWeight="Light" Foreground="{StaticResource HeadingColor}"/>
                    </StackPanel>
                </Border>

                <!-- Navigation -->
                <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto" Margin="0,0,0,10">
                    <StackPanel Margin="15,10">
                        <TextBlock Text="常规" FontSize="12" Foreground="{StaticResource DimTextColor}" FontWeight="SemiBold" Margin="10,0,0,10"/>
                        <RadioButton Style="{StaticResource NavButtonStyle}" Content="🏠  首页" IsChecked="True" Checked="Nav_Home"/>

                        <TextBlock Text="编辑工具" FontSize="12" Foreground="{StaticResource DimTextColor}" FontWeight="SemiBold" Margin="10,20,0,10"/>
                        <RadioButton Style="{StaticResource NavButtonStyle}" Content="📋  菜单编辑器" Checked="Nav_MenuEditor"/>
                        <RadioButton Style="{StaticResource NavButtonStyle}" Content="🅰️  字体编辑器" Checked="Nav_FontEditor"/>
                        <RadioButton Style="{StaticResource NavButtonStyle}" Content="🖼️  主界面编辑器" Checked="Nav_MainMenuEditor"/>
                        <RadioButton Style="{StaticResource NavButtonStyle}" Content="📝  字符串编辑器" Checked="Nav_StringEditor"/>
                        <RadioButton Style="{StaticResource NavButtonStyle}" Content="🔎  Token 查找" Checked="Nav_TokenEditor"/>
                        <RadioButton Style="{StaticResource NavButtonStyle}" Content="⌨️  键盘编辑器" Checked="Nav_KbdEditor"/>

                        <TextBlock Text="高级" FontSize="12" Foreground="{StaticResource DimTextColor}" FontWeight="SemiBold" Margin="10,20,0,10"/>
                        <RadioButton Style="{StaticResource NavButtonStyle}" Content="💉  代码注入" Checked="Nav_CodeInjector"/>
                    </StackPanel>
                </ScrollViewer>

                <!-- Footer / Global Actions -->
                <Border Grid.Row="2" Padding="20" Background="#FAFAFA" BorderBrush="{StaticResource BorderColor}" BorderThickness="0,1,0,0">
                    <StackPanel>
                        <Button Content="📂 打开 ROM" Click="OpenRom" Margin="0,0,0,10" Background="White" Foreground="{StaticResource TextColor}" BorderBrush="{StaticResource BorderColor}" BorderThickness="1"/>
                        <Button Content="💾 保存 ROM" Click="SaveRom" Background="#27AE60" Foreground="White"/>
                    </StackPanel>
                </Border>
            </Grid>
        </Border>

        <!-- Content Area -->
        <Grid Grid.Column="1">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <!-- Optional Top Bar -->
            <!--
            <Border Grid.Row="0" Height="50" Background="White" BorderBrush="{StaticResource BorderColor}" BorderThickness="0,0,0,1">
                <TextBlock Text="Dashboard" VerticalAlignment="Center" Margin="20,0" FontSize="16" Foreground="{StaticResource HeadingColor}"/>
            </Border>
            -->

            <ContentControl x:Name="MainContent" Grid.Row="1" Margin="0">
                <!-- Simple Transition Effect (WPF default doesn't allow easy triggers here without extra libs, keeping it simple) -->
            </ContentControl>
        </Grid>
    </Grid>
</Window>