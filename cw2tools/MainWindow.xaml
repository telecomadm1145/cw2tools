<Window x:Class="cw2tools.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:cw2tools"
        mc:Ignorable="d"
        Title="Chrono Cross Tool" Height="500" Width="800"
        Background="{StaticResource BackgroundColor}"
        WindowStartupLocation="CenterScreen">

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <TextBlock Text="Chrono Cross Tool" FontSize="24" FontWeight="Bold" Margin="0,0,0,20" Foreground="{StaticResource HeadingColor}" HorizontalAlignment="Center"/>

        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="2*"/>
            </Grid.ColumnDefinitions>

            <!-- File Operations -->
            <GroupBox Grid.Column="0" Header="文件操作" VerticalAlignment="Top" Margin="0,0,10,0">
                <StackPanel>
                    <Button Content="打开 Rom" Click="OpenRom" HorizontalAlignment="Stretch" Margin="0,0,0,10"/>
                    <Button Content="保存 Rom" Click="SaveRom" HorizontalAlignment="Stretch" Margin="0"/>
                </StackPanel>
            </GroupBox>

            <!-- Editors & Tools -->
            <GroupBox Grid.Column="1" Header="编辑器与工具" VerticalAlignment="Top" Margin="10,0,0,0">
                <WrapPanel Orientation="Horizontal" HorizontalAlignment="Center">
                    <Button Content="编辑菜单" Click="Button_Click" Width="140" Height="50" Margin="5"/>
                    <Button Content="编辑字体" Click="Button_Click_1" Width="140" Height="50" Margin="5"/>
                    <Button Content="编辑主界面" Click="Button_Click_2" Width="140" Height="50" Margin="5"/>
                    <Button Content="编辑字符串" Click="Button_Click_3" Width="140" Height="50" Margin="5"/>
                    <Button Content="查找Token" Click="Button_Click_6" Width="140" Height="50" Margin="5"/>
                    <Button Content="修补..." Click="Button_Click_5" Width="140" Height="50" Margin="5"/>
                    <Button Content="Debug" Click="Button_Click_4" Width="140" Height="50" Margin="5"/>
                </WrapPanel>
            </GroupBox>
        </Grid>
    </Grid>
</Window>